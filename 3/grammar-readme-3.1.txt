把上下无关文法定义在 grammar-input.txt 文本中，运行grammar.exe程序
即可读入文法到程序里，为了方便使用和后续操作，程序把每个符号都定义成一个数字，并且保存了双向的对应关系
然后程序里用数据结构存储了上下无关文法的 产生式，以及终结符号集合和非终结符号集合。
简单的输出了一下出现的符号个数和每个符号，以及终结符号集合和非终结符号集合的大小和内容到 grammar-output.txt